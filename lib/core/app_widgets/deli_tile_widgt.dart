import 'package:flutter/material.dart';

class DeliTileWidget extends StatelessWidget {
  const DeliTileWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
